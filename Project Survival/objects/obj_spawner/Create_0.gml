//Initialize variables
timer = 10*room_speed;		//duration between spawns (frames)
//timer_min = 10*room_speed;	//minimum duration between spawns 
//timer_dec = 0;		//amount to reduce from duration each spawn
alarm[0] = timer;	//set alarm to trigger spawn
num_enemies_spawned = 1;
enemiesspawn = true;